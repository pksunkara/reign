[package]
name = "integrations_views_tide"
version = "0.0.0"
authors = ["Pavan Kumar Sunkara <pavan.sss1991@gmail.com>"]
edition = "2018"
publish = false

[dependencies]
reign = { path = "../../../reign", features = ["view-tide", "json"], default-features = false }
tide = "0.8.1"
tokio = { version = "0.2.21", features = ["full"] }
serde = { version = "1.0.110", features = ["derive"] }

[dev-dependencies]
test_integrations = { path = "../../../reign/development/test_integrations" }
